import { StyleSheet } from 'react-native';
const UI = StyleSheet.create({
    card: {
      backgroundColor: "white",
      width: "100%",
      minHeight: 40,
      padding: 10,
      borderWidth: 1,
      borderRadius: 4,
      borderColor: "#ecebeb"
    }
});
export default UI
  